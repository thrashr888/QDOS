name: Release

on:
  push:
    tags:
      - "v*"

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            artifact_name: rdos
            asset_name: rdos-linux-x86_64

          - os: macos-14
            target: aarch64-apple-darwin
            artifact_name: rdos
            asset_name: rdos-macos-aarch64

          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact_name: rdos.exe
            asset_name: rdos-windows-x86_64.exe

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libasound2-dev

      - name: Build
        run: cargo build --release --target ${{ matrix.target }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.asset_name }}
          path: target/${{ matrix.target }}/release/${{ matrix.artifact_name }}

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Prepare release assets
        run: |
          mkdir -p release
          for dir in artifacts/*/; do
            name=$(basename "$dir")
            if [[ "$name" == *.exe ]]; then
              cp "$dir"/* "release/$name"
            else
              cp "$dir"/* "release/$name"
              chmod +x "release/$name"
            fi
          done
          ls -la release/

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: release/*
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # homebrew:
  #   name: Update Homebrew Tap
  #   needs: release
  #   runs-on: ubuntu-latest
  #
  #   steps:
  #     - name: Checkout tap repository
  #       uses: actions/checkout@v4
  #       with:
  #         repository: thrashr888/homebrew-qdos
  #         token: ${{ secrets.HOMEBREW_TAP_TOKEN }}
  #         path: homebrew-tap
  #
  #     - name: Get release artifacts info
  #       id: release-info
  #       run: |
  #         VERSION="${GITHUB_REF_NAME#v}"
  #         echo "version=$VERSION" >> $GITHUB_OUTPUT
  #
  #         # Download release assets to calculate SHA256
  #         curl -sL "https://github.com/thrashr888/QDOS/releases/download/$GITHUB_REF_NAME/rdos-macos-aarch64" -o rdos-macos-aarch64
  #         curl -sL "https://github.com/thrashr888/QDOS/releases/download/$GITHUB_REF_NAME/rdos-macos-x86_64" -o rdos-macos-x86_64 || true
  #
  #         SHA_ARM=$(shasum -a 256 rdos-macos-aarch64 | cut -d' ' -f1)
  #         echo "sha_arm=$SHA_ARM" >> $GITHUB_OUTPUT
  #
  #         if [ -f rdos-macos-x86_64 ]; then
  #           SHA_INTEL=$(shasum -a 256 rdos-macos-x86_64 | cut -d' ' -f1)
  #           echo "sha_intel=$SHA_INTEL" >> $GITHUB_OUTPUT
  #         fi
  #
  #     - name: Update formula
  #       env:
  #         VERSION: ${{ steps.release-info.outputs.version }}
  #         TAG: ${{ github.ref_name }}
  #         SHA_ARM: ${{ steps.release-info.outputs.sha_arm }}
  #         SHA_INTEL: ${{ steps.release-info.outputs.sha_intel }}
  #       run: |
  #         # Generate formula from template with variable substitution
  #         envsubst < .github/templates/rdos.rb.template > homebrew-tap/Formula/rdos.rb
  #
  #     - name: Commit and push
  #       run: |
  #         cd homebrew-tap
  #         git config user.name "GitHub Actions"
  #         git config user.email "actions@github.com"
  #         git add Formula/rdos.rb
  #         git commit -m "Update rdos to ${{ steps.release-info.outputs.version }}"
  #         git push
