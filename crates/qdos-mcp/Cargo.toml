[package]
name = "qdos-mcp"
version = "0.18.0"
edition = "2021"
description = "MCP (Model Context Protocol) client implementation for R-DOS"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
